<% if (page.total > 1){ %>
    <nav class="pagination">
        <%- paginator({
            // prev_text: "&laquo; Anterior",
            // next_text: "Próximo &raquo;"
            prev_next: false,
            end_size: 1,
            mid_size: 2,
        }) %>
    </nav>
<% } %>